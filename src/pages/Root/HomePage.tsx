import { ScrollArea } from '@/components/Scrollarea/Scrollarea';
type HomePage = {};

export const HomePage: React.FC<HomePage> = () => {
   return (
      <section>
         <ScrollArea />
      </section>
   );
};
